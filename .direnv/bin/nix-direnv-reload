#!/usr/bin/env bash
set -e
if [[ ! -d "/home/curstantine/Code/Personal/curstantine.nix" ]]; then
  echo "Cannot find source directory; Did you move it?"
  echo "(Looking for "/home/curstantine/Code/Personal/curstantine.nix")"
  echo 'Cannot force reload with this script - use "direnv reload" manually and then try again'
  exit 1
fi

# rebuild the cache forcefully
_nix_direnv_force_reload=1 direnv exec "/home/curstantine/Code/Personal/curstantine.nix" true

# Update the mtime for .envrc.
# This will cause direnv to reload again - but without re-building.
touch "/home/curstantine/Code/Personal/curstantine.nix/.envrc"

# Also update the timestamp of whatever profile_rc we have.
# This makes sure that we know we are up to date.
touch -r "/home/curstantine/Code/Personal/curstantine.nix/.envrc" "/home/curstantine/Code/Personal/curstantine.nix/.direnv"/*.rc
